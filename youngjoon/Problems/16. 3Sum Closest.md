---
layout: post
title: 16. 3Sum Closest
category: leetcode
date: 2026-05-16 00:00:00 +0900
description: https://leetcode.com/problems/3sum-closest/description/
img: leetcode.png # Add image post (optional)
fig-caption: # Add figcaption (optional)
status: #failed or success
---

# 16. 3Sum Closest

Given an integer array nums of length n and an integer target, find three integers at distinct indices in nums such that the sum is closest to target.

Return the sum of the three integers.

You may assume that each input would have exactly one solution.

 

Example 1:

Input: nums = [-1,2,1,-4], target = 1
Output: 2
Explanation: The sum that is closest to the target is 2. (-1 + 2 + 1 = 2).
Example 2:

Input: nums = [0,0,0], target = 1
Output: 0
Explanation: The sum that is closest to the target is 0. (0 + 0 + 0 = 0).

---

선행 문제인 15. 3Sum 을 이해 했는지 물어보는 문제. 

문제의 차이가 크게 없기 떄문에 풀이 생략.

---

### Solution

```cpp
class Solution {
public:
    int threeSumClosest(vector<int>& nums, int target) {
        sort(nums.begin(),nums.end());
        int results = nums[0]+nums[1]+nums[2];

        for(int i=0; i<nums.size(); i++){
            int left = i+1;
            int right  = nums.size()-1;
            while(left<right){
                int cur_val = nums[i]+nums[left]+nums[right];
                if(cur_val == target) return cur_val;
                else if(cur_val > target){
                    if(abs(cur_val-target)<abs(results-target)) results = cur_val;
                    right--;
                }
                else {
                    if(abs(cur_val-target)<abs(results-target)) results = cur_val;
                    left++;
                }
            }
        }
        return results;
    }
};
```