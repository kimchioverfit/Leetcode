---
layout: post
title: Basic
category: 
date: 2025-00-00 00:00:00 +0900
description: 
img: leetcode.png # Add image post (optional)
fig-caption: # Add figcaption (optional)
status: #failed or success
---

# 

```cpp
class Solution {
public:
    int areaOfMaxDiagonal(vector<vector<int>>& dimensions) {
        long long bestDiag2 = -1; // 대각선^2의 최댓값
        int bestArea = 0;         // 해당 대각선에서의 최대 면적

        for (const auto& d : dimensions) {
            long long w = d[0], h = d[1];
            long long diag2 = w*w + h*h; // pow 대신 정수 곱셈
            int area = static_cast<int>(w * h);

            if (diag2 > bestDiag2) {
                bestDiag2 = diag2;
                bestArea = area;
            } else if (diag2 == bestDiag2) {
                if (area > bestArea) bestArea = area;
            }
        }
        return bestArea;
    }
};
```
